'use client';

import { OrgDashboardPage } from '@/components/teacher/org-dashboard';

/** Org-manager home dashboard (stats + quick links), at /org. */
export default function OrgHomePage() {
  return <OrgDashboardPage />;
}
