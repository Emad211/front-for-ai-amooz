// Managers share the same settings (profile/security/notifications/appearance)
// as teachers; re-export it under /org so the manager panel keeps its URL space.
export { default } from '@/app/(teacher)/teacher/settings/page';
