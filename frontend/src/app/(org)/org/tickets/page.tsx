// Managers share the same support-ticket experience as teachers; re-export it
// under /org so the manager panel keeps its own URL space.
export { default } from '@/app/(teacher)/teacher/tickets/page';
