# Jalali date picker theme fix

- Replaced mixed table/grid calendar layout with a stable seven-column table layout.
- Made day cells use the shared theme tokens in both light and dark modes.
- Kept month navigation and selected, current, outside, and disabled-day states inside the picker surface.
